# Conda environment for the backend (optional — plain venv + pip works; no Stan/CmdStan required).
# Create:    conda env create -f backend/environment.yml
# Activate: conda activate sepm-crop
name: sepm-crop
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
    - -r requirements.txt
