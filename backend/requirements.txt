# Backend stack — install: pip install -r backend/requirements.txt
# Prefer Python 3.11. On Windows, Prophet/CmdStan is unreliable with Store Python alone — use conda (backend/environment.yml) or python.org + venv.
fastapi[all]==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic==2.9.2
pydantic-settings==2.6.1
alembic==1.14.0
httpx==0.27.2
# Prophet / CmdStan — stanio is a cmdstanpy dependency; pin range avoids surprise resolver drift on git pull
prophet==1.1.6
cmdstanpy==1.2.5
stanio>=0.3.0,<0.6
holidays>=0.34,<1
plotly==6.7.0
xgboost==2.1.3
scikit-learn==1.5.2
pandas==2.2.3
numpy==1.26.4
python-dotenv==1.0.1
APScheduler==3.10.4
